- run prometheus: 

 docker run -d \
  --name prometheus \
  -p 9090:9090 \
  -v $(pwd)/test-k6/prometheus.yml:/etc/prometheus/prometheus.yml \
  prom/prometheus

- run grafana: 

docker run -d \
  --name grafana \
  -p 5000:3000 \
  grafana/grafana