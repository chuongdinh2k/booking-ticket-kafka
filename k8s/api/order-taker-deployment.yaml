apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-taker-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-taker-service
  template:
    metadata:
      labels:
        app: order-taker-service
    spec:
      containers:
        - name: order-taker-service
          image: stephendinh/booking-system-order-taker:1
          command: ['node', 'dist/apps/orders-taker/main']
          ports:
            - containerPort: 3001
          env:
            - name: REDIS_HOST
              value: 'redis'
            - name: REDIS_PORT
              value: '6379'
            - name: DB_HOST
              value: 'mysql'
            - name: DB_PORT
              value: '3306'
            - name: DB_USERNAME
              value: 'root'
            - name: DB_PASSWORD
              value: 'admin123'
            - name: DB_DATABASE
              value: 'booking_tickets'
            - name: KAFKA_BROKER
              value: 'kafka:29092'

---
apiVersion: v1
kind: Service
metadata:
  name: order-taker-service
spec:
  selector:
    app: order-taker-service
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer
